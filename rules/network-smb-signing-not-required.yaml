schema_version: "1"
rule_type: network
id: "NETWORK-SMB-SIGNING-NOT-REQUIRED"
name: "SMB signing is not required"
metadata:
  author: "sangkan-dev"
  license: "MIT"
  last_verified: "2026-06-14"
compatibility:
  minimum_temu_version: "1.5.0"
  required_capabilities: ["network_service"]
protocols: ["smb"]
severity: medium
cvss: 5.9
risk_level: safe
requires_confirmation: false
matcher:
  required_signals: ["smb_signing_not_required"]
remediation: "Require SMB signing on clients and servers."
references:
  - "https://learn.microsoft.com/en-us/windows-server/storage/file-server/smb-signing-overview"
